extends ../adminlay

block content
	.container
		.row
			.col-xs-8.col-md-6.col-xs-offset-2.col-md-offset-3.adminlog 
				form(method="post",action="/user/signin")
					.input-wrapper
						input(type="text",name="user[name]",placeholder="手机号")
					.input-wrapper
						input(type="password",name="user[password]",placeholder="密码")
					.input-wrapper
						input(type="text",name="",placeholder="请填写验证码")
						div.captcha-container 获取短信验证码
					.actions
						button.btn(type="submit") 登录